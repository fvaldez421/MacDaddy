import React from "react";
import './Header.css';

const Header = (props) => (
    <div className="jumbotron">
        <h2>
            This is MacDaddy (Header)
        </h2>
    </div>
)

export default Header;